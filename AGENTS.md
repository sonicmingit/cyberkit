# 喵伴 2.0 智能体协作与持续开发规则

本文件适用于本仓库内的代码、配置、资源、脚本和项目文档。目标是在现有 CyberKit 固件上持续迭代，让下一位智能体能确认现状、沿用有效实现、完成验证并留下可继续开发的记录。除特别说明，以下路径均相对本仓库根目录。

基线核对日期：2026-09-07。本文件中的现状是该日期的快照，每次开发仍须检查实际代码和配置。用户当前明确要求优先于本文件；不能把历史计划当成当前任务授权，自动执行全部路线图、推送或发布。

## 1. 开始工作：先确认项目和证据

1. 确认工作目录是 `2.0/cyberkit/`，读取本文件及任务目录中更具体的 `AGENTS.md`。
2. 执行 `git status --short`、`git branch --show-current`、`git remote -v`，记录当前分支和已有修改；不得覆盖、回滚或顺手提交用户的无关改动。
3. 阅读 `文档/00-文档索引.md`、`01-项目需求记录.md`、`02-开发日志.md`、`07-迭代记录/00-迭代总览.md` 和当前迭代文档。
4. 按下表读取专项文档，再追踪真实源码调用链。使用 `rg` 定位定义、调用方、资源引用、配置和构建入口。
5. 对实现任务，先在现有迭代中明确需求编号、范围、验收和文件级方案，再做小步修改；已有记录足够时更新原记录，不重复建档。

| 工作内容 | 必读依据（均在 `文档/`） |
|---|---|
| 全局规划与协作 | `03-总体开发路线图.md`、`05-上游同步与提交规范.md` |
| 板级适配、现有功能 | `08-赛博喵伴2.0现有功能分析.md` |
| 车载模式 | `04-车载模式需求基线.md`、`06-车载模式开发说明.md`、`09-车载模式迁移与实施方案.md`、`10-旧版车载模式复用基线.md` |
| 表情资源 | `11-车载表情资源筛选与转换记录.md` |
| 菜单、触摸、计时器入口 | `12-圆形下拉菜单原型说明.md`、`07-迭代记录/02-功能入口与通用菜单基线.md` |
| 唤醒词 | `13-默认唤醒词配置说明.md` |

### 证据与冲突处理

- “当前做了什么”以实际源码、有效配置和本次验证为准；“应该做什么”以当前用户要求及最新需求变更为准。源码中仍有旧功能不代表应继续保留旧产品方案。
- 需求记录与已更新的专项要求优先于历史迁移建议；发现冲突时在迭代记录写明取舍依据，不静默更改需求编号或验收口径。
- `../初步分析.md` 是早期草稿，其 `开发记录/README.md` 链接已过时；当前项目记录集中在 `文档/`。
- 已废弃 `DUMMY -> POMODORO -> SLEEP -> CAR_MODE` 循环菜单方案。`09`、`10` 中残留的“仅 CAR_MODE 页面激活才运行”等建议不得覆盖当前的服务独立生命周期要求。
- 原型、候选资源、旧版测试通过记录、建议模块名都不等于本仓库已实现。不要把旧版 62/62 测试结果作为 V2.0 验收证据。

## 2. 当前基线与开发边界

- 目标板：`CyberVoc-Board-V2_0`；ESP32-S3、360×360 圆形屏、LVGL/Emote、QMI8658A 六轴 IMU。
- 现有功能包括语音对话、AFE 唤醒、Wi-Fi/ML307 网络、OTA、屏幕和头部触摸、DOA、底座、电量监控、番茄钟和睡眠闹钟。
- 当前 `ui_bridge` 已接入 HOME、圆环菜单、计时器、车载设置、设备信息页；旧番茄钟/睡眠入口不再注册。新增入口复用 `feature_ui` 菜单项模型。
- 当前分支 `feature/car-mode`；本轮审查修复详见 `文档/07-迭代记录/03-审查修复与菜单扩展.md`。功能已实现，是否通过构建/硬件验收以该迭代证据为准，不能沿用首版结论。
- 默认只适配 V2.0。修改共享代码时必须评估 V1.2 等其他板型，使用板级实现或适当配置隔离，避免把 V2.0 头文件无条件引入公共层。
- 相邻旧版工程仅作为有选择的迁移参考；不得整文件覆盖新板级实现，不复制旧 GPIO、BMI270 驱动、分区或构建参数。

## 3. 代码导航与模块职责

| 位置 | 职责与修改约束 |
|---|---|
| `main/boards/CyberVoc-Board-V2_0/CyberVoc.cc/.h` | 板级初始化、硬件接入、现有 IMU 任务；保留装配职责，不继续堆叠菜单和运动判断业务 |
| 同目录 `ui_bridge.*`、`touch_cst816s.*`、`touch_sensor.*` | 页面生命周期、触摸和输入分发；先查现有调用链，再增加菜单和手势 |
| 同目录 `customer_ui/alarm_*` | 番茄钟、睡眠闹钟、共享管理和结束提示；移除前检查全部依赖 |
| 同目录 `cybervoc_tools.*` | 板级 MCP 工具入口；与 UI 共用业务服务 |
| 同目录 `audio_analysis.*`、`base_control.*` | DOA 和底座动作；车载模式需处理冲突，不能无意触发电机 |
| `main/application.*` | 应用状态、协议事件与 `Application::Schedule()` 调度 |
| `main/display/emote_display.*` | 表情、临时动画、显示恢复；共享代码更改需评估全部调用方 |
| `main/audio/`、`main/protocols/`、`main/mcp_server.*` | 音频、唤醒、通信与通用工具框架；避免为板级功能重写公共流程 |
| `main/settings.*` | 现有持久化封装；新增设置应优先复用 |
| `main/CMakeLists.txt`、板级 `config.json`、`sdkconfig.defaults*` | 源码纳入、板型和默认配置；确认实际生效路径 |
| 板级 `assets/`、`scripts/`、`partitions/v2/16m.csv` | 资源、转换和打包、容量与升级布局 |

已有 `car_mode/car_motion.h` 纯算法、`car_mode_service.cc` 服务、`customer_ui/timer_state.h` 与 `timer_service.cc`；页面在 `feature_ui.cc`。不要另建第二套服务。算法不依赖 LVGL/ESP-IDF，使回放测试可在主机运行。V2.0 板级 `.c/.cc` 已由 CMake 递归收集；新增其他后缀或公共目录文件时另查构建规则，不假定自动纳入。

构造与任务约束：`feature_ui_create()` 构造期间不能调用 `Board::GetInstance()`；服务在板构造完成后通过应用队列启动。所有 UI/MCP 设置写入和计时器命令在应用任务执行，LVGL PSRAM 栈不得直接写 NVS。显示/音频调用前释放服务状态锁，避免锁顺序反转。

屏幕触摸实际走 `Cst816sTouch` + LVGL adapter；板级回调保留 IRQ 唤醒并在按下期间补读释放，勿删除丢中断保护或修改托管驱动来实现同一逻辑。头部触摸走 GPIO8 的 `HeadTouchGpioTask`，不是未使用的电容 `touch_sensor.cc`。

## 4. 所有新功能共同遵守的规则

1. **先适配，再扩展。** 实现前列出入口、状态来源、调用方、任务/定时器、资源、NVS、MCP、退出及错误路径；优先复用现有调度、设置、音频、显示和协议能力。
2. **服务是状态的唯一来源。** 菜单、详情页和 MCP 调用同一服务接口。不得分别保存互不一致的启用状态或计时状态；服务变更成功后再更新 UI，失败需反馈原因。
3. **生命周期完整。** 定义初始化、启用、暂停/被抢占、恢复、关闭和异常处理。重复开启/关闭不重复创建任务；销毁页面前停止关联回调，处理已排队回调的有效性，避免悬空引用。
4. **服务运行与页面可见性分离。** 收起菜单、进入详情或返回主页只改变展示；关闭功能才清理其业务运行状态。页面被遮挡时可暂停渲染，但仍正确计时或维护模式状态。
5. **跨任务更新有边界。** 传感器和网络回调只投递业务事件；沿用应用调度和项目实际 LVGL 锁/上下文约定。`Schedule()` 不自动替代所有 LVGL 线程保护。
6. **实时链路有界。** 采样、触摸和音频回调不执行耗时文件读写、网络等待或整段动画。高频状态使用最新状态槽和有界调度，防止事件队列积压；记录丢弃、超时和异常的必要诊断。
7. **配置可迁移。** 明确默认值、取值范围、NVS 命名空间/键、版本兼容、旧键处理和恢复出厂行为；检查写入结果，不在采样/刷新循环频繁写 Flash。不为功能迁移清空全机 NVS。
8. **MCP 与 UI 行为一致。** 校验参数范围、不可用状态和重复调用；不把测试注入事件当成真实传感器来源。移除旧功能时同步处理旧工具注册，不能留下仍可触发旧任务的隐藏入口。
9. **失败可恢复。** 资源缺失、设备不存在、读写失败或数据过期时降级并提供状态；不以崩溃、永久动画占用或静默成功掩盖失败。
10. **改动聚焦。** 沿用相邻代码风格和现有依赖；不要顺手格式化全仓库、升级 SDK、替换驱动或修改第三方组件。确需修改公共组件时记录原因和回归范围。

## 5. 功能专项规则

### 5.1 通用下拉圆环菜单（需求-003）

- 以时间主页下拉展开、单击操作、长按详情、上滑收起为交互基准；中心数字时间持续刷新，保留日期和简短状态。
- 采用统一菜单项模型：稳定标识、图标、名称、类型、状态、禁用态、单击与长按回调。开关、入口、入口/状态项目的反馈可区分。
- 按用户最新变更，六项为 CAR、TIMER、HEAD、INFO、SET、一个禁用预留槽。HEAD 控制 GPIO8 头部触摸，不关闭屏幕触摸；历史“三个预留槽”已被新增功能占用。
- 按 360×360 圆形安全区计算布局与触控命中，使用半径和扇区角度，不直接复制 Figma 500×500 展示尺寸或用重叠矩形热区替代。
- 明确滑动与点击的阈值、事件消费和长按释放策略，防止一次长按同时触发开关；处理与旧边缘切页、页面滚动、唤醒手势的冲突。
- 新功能通过注册接入，不为每项功能添加专用布局和手势分支。展开、收起和选中动画应轻量且不阻塞实时任务。
- 分区角度、旋转选择和 12/24 小时制尚未最终确定；实现前记录所采用口径，不自行宣称已获产品确认。

### 5.2 车载模式（需求-001）

- 独立状态机与服务管理启用、校准、正常工作和传感器异常；开机默认/记忆策略尚待确认，不擅自默认上电启用。
- 首期覆盖巡航、等待、加速、减速、急刹、颠簸和传感器异常。左右转需可信且已验证的输入、方向及超时策略；倒车必须有可信来源，不能从单纯 IMU 加速度推断挡位。
- 不宣称 IMU 可直接判断绝对车速、点火、踏板或静止/匀速的真实车辆状态。测试注入和推断事件在日志中标明来源。
- 迁移旧版纯 C++ 检测核心与回放思路，重新适配 QMI8658 量程、字节序、g/°/s 单位和坐标轴。当前配置 `CTRL1=0x40`，显式小端/地址自增并回读配置；读取时核对 STATUS0 和采样计数器，不能恢复为 BE=1 的 `0x60` 后仍小端解码。
- 当前 IMU 任务 20 ms 调度（目标 50 Hz）；芯片 ODR 56.05 Hz，读取新鲜样本，失败 3 秒重试初始化。合并原 shake 采样路径，同一 IMU 不开两个竞争读取任务；保持共享 I2C、电量和触摸可用。
- 校准、滤波、持续确认、迟滞、冷却、优先级和断流恢复必须完整。旧版阈值仅作调试起点；先验证静态约 1g、陀螺接近零，再使用台架/运动回放和实机数据标定。
- 采样只更新最新业务状态，显示以有界频率消费；可参考约 100 ms 刷新、最多一个待调度 UI 更新，避免每个样本排一个动画任务。
- 车载路径只改变屏幕，不自动驱动底座。核对 `SetEmotion()` 及相关回调的动作联动，提供经验证的纯显示适配路径。
- `InsertAnimDialog()` 仅有一个临时槽，不能由车载循环持续抢占。系统告警、Listening/Speaking 等优先于普通车载表情；短时事件结束后根据当前状态恢复，不能恢复过期快照。
- DOA、晃动唤醒、头部触摸、底座临时动画的冲突策略需显式定义；临时抑制不能破坏用户持久配置，退出后按当前有效设置恢复。

### 5.3 用计时器替换番茄钟和睡眠闹钟（需求-002）

- 先列出页面、菜单、任务、事件、MCP、音频、资源、NVS 和构建引用；仅移除旧功能独占部分，保留系统时钟和共享提醒能力。
- 产品入口和正常业务流程不再出现番茄钟、睡眠闹钟；不能只隐藏页面而保留后台任务和工具入口。
- 新计时器支持设置、开始、暂停、继续、停止、取消与结束提醒；后台运行不依赖页面对象存活。
- 以单调时间计算经过/剩余时间，避免按 UI 刷新次数递减，也避免系统时间校准导致计时跳变。结束提醒只触发一次；暂停、取消、重设和并发事件不得留下过期提醒。
- 最大时长、时间误差验收值、单击行为、提醒方式及重启恢复策略需在实现前明确。未定部分写入待确认项，继续可独立完成的依赖分析和基础工作。

### 5.4 双默认唤醒词（需求-004）

- 默认词条为 `你好,小特`、`小特小特`。按需求保留 ASCII 逗号；若模型元数据不同，明确识别词条与显示/协议文本映射。
- 用户允许容量不足时优先“小特小特”；当前 V2.0 固定选择 `wn9_xiaotexiaote_tts2`，关闭旧喵伴模型，AFE 检查模型名称并记录真实词条。现有依赖没有“你好,小特”模型，不得宣称双词支持。
- 核对模型版本、实际元数据、构建选择、分区容量和烧录路径，验证两个词条均可识别且上报可区分。
- 修改 `WakeWordInvoke("你好")` 等按键/触摸字符串不等于更换麦克风唤醒模型。
- 缺少可用模型时明确记录阻塞及日志策略，不伪造模型或标为完成；继续可独立进行的模型加载与配置路径核查。
- 覆盖安静、扬声器播放、风噪/车载噪声、重启、OTA、恢复出厂与用户配置迁移，记录误触发和漏检的测试条件及结果。

## 6. 表情、资源、Flash 与 OTA

- 用户允许空间不足时仅保留 TITA、猫。当前打包这两套并提供 PACK 运行时切换；`car_packs/` 保留原始来源，`scripts/prepare_v2_assets.py` 在 build 下生成抽帧资源，不改原素材。双眼包未打包。
- 逻辑事件与资源名解耦，缺少专用表情时明确中性回退。TITA 候选缺少部分加减速专用动画，不能把不匹配素材强行标成完整功能。
- 转换资源保留来源、工具/参数、尺寸、帧时长、大小和校验清单；结构/RLE 回读通过后仍需实机检查颜色、方向、裁切、帧率和内存。
- 未选择的表情包不能无条件进入最终资源镜像。检查 Emote 登记、layout、资源名称长度和实际打包规则，不能仅把文件复制到 assets 目录就视为接入完成。
- 当前 `partitions/v2/16m.csv`：`ota_0=6000K`、`ota_1=3000K`、`assets=5500K`、`model=300K`。以实际生效分区表和生成镜像重新测量，源文件合计约 5.32 MB 不是镜像体积。
- OTA 槽不对称，核查 `main/ota.*` 和压缩 OTA/bootloader 的真实升级链路：记录应用、升级包及解压后镜像各自写入哪个槽。不能仅以 app 小于 6000K 判为可升级，也不能忽略压缩路径直接假设两个槽约束相同。
- 容量不足优先精简资源、降低帧数/时长并测量解码负载。不擅自改变分区、bootloader 或安全配置；需要变更时先形成迁移、保留配置、刷写、回退和兼容验证方案。
- 避免高频闪烁；夜间亮度不随事件强度突然变化。

## 7. 构建与验证

### 构建前

- 在本仓库目录运行命令。README 要求 ESP-IDF >=5.4，现有记录建议 5.5.2；版本建议不是本机已验证环境，不沿用旧版工程的 IDF 6.0.2 结论。
- 核对 `README.md`、`main/idf_component.yml`、依赖锁定文件（若存在）、板级 `config.json`、`sdkconfig.defaults*` 和最终 `sdkconfig`，记录工具链版本与配置来源。
- Windows/OneDrive 下先确认源码和资源实际可读取；环境不全时记录具体缺失项，不通过删除依赖锁或批量升级组件掩盖问题。

```powershell
# 从 2.0/cyberkit/ 执行；先加载本机实际安装的 ESP-IDF 环境
git status --short
idf.py --version
python --version
python scripts/release.py --list-boards
python scripts/release.py CyberVoc-Board-V2_0
```

**构建脚本的已知行为：** `scripts/release.py` 在同版本 `releases/v<版本>_<板型>.zip` 已存在时会直接跳过；仓库已有发布包。先检查输出，必要时将对应旧包备份到明确的安全位置，再执行正式构建，或在已确认板级配置正确的环境中用 `idf.py build` 验证。不得把“跳过”或已有 ZIP 当作本次编译成功，也不得为绕过跳过逻辑随意增加产品版本号。脚本会调用 `set-target` 并重建配置，运行前检查并保留用户配置改动。

### 按改动选择验证

| 改动 | 必要验证 |
|---|---|
| 纯文档 | 路径/链接、需求一致性、Markdown 和差异检查；无需编译固件 |
| 纯算法、状态机、计时 | 主机测试/回放覆盖边界、迟滞、超时、抢占、取消和恢复，再进行板级编译 |
| 菜单/触摸/UI | 正式板级编译，360×360 实机验证裁切、命中、长按不误点击、展开收起和状态一致性 |
| IMU/底座/电源 | 编译，型号/轴向/采样台架日志，断流/异常恢复及共享总线回归，必要的实机数据验证 |
| EAF/模型/打包 | 实际生成镜像、容量和加载验证；EAF 实机播放或双词条语音测试 |
| 公共层/SDK/OTA | 受影响板型构建与基础功能回归，实际升级路径及回退验证 |

- 功能回归至少关注主页、菜单、语音 Listening/Speaking、唤醒、联网、OTA、触摸、底座、DOA、电量，以及功能退出后的恢复；按实际影响选择测试，不能机械宣称全部通过。
- 高风险状态覆盖反复启停、快速切页、同时发生的语音/提醒/传感器事件、资源缺失、断流与长期运行。持续运行记录时长、可用堆/栈余量或相关异常证据。
- 记录实际命令、退出结果、构建板型、源码提交/未提交状态、产物路径和尺寸。测试程序、日志和结果必须对应当前实现。
- 未编译功能不标“已完成”；硬件不可用则明确“待实机验证”，给出可复现步骤。环境失败、未运行、静态检查和实测通过分别记录。

## 8. Git、上游同步与交付边界

- 默认 `origin=https://github.com/sonicmingit/cyberkit.git`，`upstream=https://github.com/cyberai8/cyberkit.git`；操作前复核实际远程。
- `main` 保持可发布；当前车载迭代在 `feature/car-mode`。新分支根据任务范围命名，不为读文档或微小修订强制切分支。
- 一次提交解决一个清楚问题，提交说明写出具体功能与原因。提交前检查差异和 `git diff --check`，只暂存本任务相关文件。
- 不提交构建缓存、临时日志、无关生成文件或密钥；正式资源与发布包按仓库已有约定和本次范围管理。测试签名材料不能当作生产密钥使用。
- 上游同步遵循 `文档/05-上游同步与提交规范.md`：干净工作区 fetch、查看差异、合入 main、构建回归，再合入开发分支并记录冲突。没有同步任务时不自动执行合并。
- 保留官方历史，不对共享分支强推。提交、推送、烧录、发布按用户本次或已明确的持续授权执行，不能仅因为历史迭代写了“推送”就触发外部发布。

## 9. 持续文档与智能体交接

- 本文件及兼容入口 `agment.md` 是仓库级开发入口；需求、计划、分析、日志等新增项目记录统一放 `文档/`，采用“数字序号-中文名称”，不写入官方 `docs/`。
- 本文件维护长期规则和准确导航，详细进度归需求与迭代文档；代码结构或构建约束变化时同步修订本文件，避免维护第二套过时状态表。
- 需求状态采用索引定义：待细化、已计划、开发中、待验证、已完成、已阻塞。历史“已细化”表示需求已细化，绝不代表功能完成；状态名称需调整时保留变更说明。
- 迭代状态遵循迭代总览的独立口径：计划中、进行中、待实机验证、已完成、已阻塞。不要把需求状态和迭代状态混用。
- 每次实现更新对应需求状态、迭代记录和 `02-开发日志.md`；新增文档更新索引，范围或阶段变化同步路线图。文档维护任务只记录实际文档成果，不提升固件功能完成状态。
- 提交哈希只在实际提交后填写；尚未提交写“未提交”，没有实机证据写“未实机验证”，不得虚构测试或提交。

迭代记录至少包含：

```text
关联需求 / 迭代编号：
目标、范围、非目标：
当前状态与基线（分支、提交、环境）：
适配依据与调用链：
修改文件与实际行为：
验证命令、结果、产物与尺寸：
实机型号、固件版本、步骤与结果（或未验证原因）：
需求变更/待确认决策：
提交/推送情况：
已知问题、阻塞与下一步：
```

结束任务时用中文说明已改什么、验证到哪一步、哪些仍未完成及下一步入口。下一位智能体应能从记录继续执行，而无需猜测代码位置、复用条件、测试证据或待定产品决策。
