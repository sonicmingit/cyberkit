# 猫眼设计稿 v1 动画转换

本目录包含 16 个车载状态：`gif/` 用于预览，`eaf/` 用于赛博喵伴 2.0 实机验证，`全部表情预览.gif` 用于快速选型。

当前文件是由每行四个设计关键姿态生成的“可烧录原型”，不是最终 20 FPS 补间动画。每个 EAF 为 360×360、8-bit 索引色 BGRA 调色板、RLE 编码、12×30 行分块。单包 EAF 合计约 0.89 MiB。

在确定最终表情包后，再对常用状态补间，可避免三套表情同时占满约 5.5 MB 的资源分区。`eaf/manifest.json` 含帧数、大小、逐帧时长和 SHA-256。
